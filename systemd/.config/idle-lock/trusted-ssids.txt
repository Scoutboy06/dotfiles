# Trusted SSIDs for idle-lock-auto
#
# When connected to one of these networks, hypridle will be stopped
# (no automatic screen lock). On other networks or when disconnected,
# hypridle will run normally.
#
# Format: one SSID per line. Blank lines and lines starting with # are ignored.
#
# Example:
# MyHomeWiFi
# Office-5G
