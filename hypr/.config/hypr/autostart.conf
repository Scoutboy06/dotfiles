# Environment variables
env = EDITOR,nvim

# Core services
exec-once = elephant                                      # App launcher backend (for walker)

# Extra autostart processes
# exec-once = uwsm app -- my-service

exec-once = uwsm app -- ~/p/dotfiles/check-status.sh
exec-once = uwsm app -- ~/p/dotfiles/check-agents-sync.sh
exec-once = uwsm app -- 1password
exec-once = uwsm app -- voxtype daemon
