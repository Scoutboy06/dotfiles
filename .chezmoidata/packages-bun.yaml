# Bun global packages (installed via `bun add -g`)
#
# Each item is a package with fields:
# name:         The package name (REQUIRED)
# device:       all|eliaspc|eliaslt|worklt|server (REQUIRED)
# requires_de:  true|false (REQUIRED)
# ignore_scripts: true|false (optional, default false)
#
packages:
- name: "@earendil-works/pi-coding-agent"
  device: all
  requires_de: false
  ignore_scripts: true
