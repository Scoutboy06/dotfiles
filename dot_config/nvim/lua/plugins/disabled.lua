-- Disable some LazyVim default plugins if needed
-- Useful for customization or reducing startup time

return {
  -- Example: disable a plugin
  -- { "nvim-neo-tree/neo-tree.nvim", enabled = false },
}
