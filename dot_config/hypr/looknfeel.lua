-- Personal appearance overrides loaded after Omarchy's defaults.

hl.config({
  general = {
    gaps_in = 4,
    gaps_out = 8,
    resize_on_border = true,
    resize_corner = true,
  },

  decoration = {
    rounding = 8,
  },
})
