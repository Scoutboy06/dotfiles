#!/bin/bash
set -euo pipefail

# Returns the battery percentage remaining as an integer.
# Used by battery-monitor and waybar.

upower -i "$(upower -e | grep BAT)" 2>/dev/null | awk '/percentage/ { 
    print int($2)
    exit
}'
