# Python pip packages (optional, for specific scripts)
# Install with: pip install --user -r manifests/pip.txt
# Or use pipx for isolated installs

# Required by: bin/.local/bin/md-convert
markdownify

# Required by: agents/skills/pptx/* (Office document manipulation)
defusedxml
Pillow
